{
  "name": "doc-mgmt-extraction-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AI document extraction via Gemini API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
